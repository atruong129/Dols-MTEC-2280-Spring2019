// Anthony Truong drawing hw second
// Thanks to processing drawing reference for giving me ideas



void Setup() 
{
size(900, 900);
background (0);
}
void draw () // looping
{
 // ellipse (mouseX, mouseY, 50, 50);
fill (255);
ellipse(mouseX, mouseY, 50, 50);
if (mousePressed && (mouseButton == LEFT))
fill (random ( 0, 255), random (0, 225), random (0, 225));
  ellipse(mouseX, mouseY, 50 , 50);

}
